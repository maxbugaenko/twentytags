<?
require_once('Model/Facebook/facebook.php');
$config = array(
    'appId' => '706201236078444',
    'secret' => 'fabe992657dcffefd59099502cdf0ce9',
    'allowSignedRequest' => false // optional but should be set to false for non-canvas apps
);
$facebook = new Facebook($config);
?>

<div class="menu-container" id="menu-container">
    <div class="controls-section">
        <?=$this->mainTag?> <i id="tags-button" class="btn-icon fa fa-bars"></i>
    </div>
    <div class="logo-section">
        <a href="<?=$this->url(array(), "browse", false)?>">
            <img src="<?=$this->staticFile("images/logo.png")?>"/>
            <span>TwentyTags</span>
        </a>
    </div>
    <div class="profile-section">
        <? if ($this->user):?>
            <?=$this->user->name?>
            <?$logoutUrl = $this->url(array("action"=>"logout"), "logout", false);?>
            <?$followingUrl = $this->url(array(), "following", true);?>
            <?$feedUrl = $this->url(array(), "feed", true);?>
            <?$addTagUrl = $this->url(array(), "addtag", true);?>
            <i onclick="location.href='<?=$feedUrl?>'" class="btn-icon fa fa-refresh"></i>
            <i onclick="location.href='<?=$followingUrl?>'" class="btn-icon fa fa-th"></i>
            <i onclick="location.href='<?=$addTagUrl?>'" class="btn-icon fa fa-plus"></i>
            <i title="Sign out" onclick="location.href='<?=$logoutUrl?>'" class="btn-icon fa fa-external-link"></i>
        <?else:?>
            <?
                $loginUrl = $facebook->getLoginUrl(array(
                    "redirect_uri" => WEBSITE_URL.$this->url(array("action"=>"login"), "login", false)
                ));
            ?>
            Sign in
            <i title="Sign up with Facebook" onclick="location.href='<?=$loginUrl?>'" class="btn-icon fa fa-facebook-square"></i>
        <?endif?>
    </div>
</div>
<script>
    $("#tags-button").click(function() {
        $("#menu-panel").toggleClass("slide-menu-right");
        $("#page-container").toggleClass("slide-page-container-right");
        $("#menu-container").toggleClass("menu-container-slide-right");
    });
</script>