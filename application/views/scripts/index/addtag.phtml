<?$this->headTitle("TwentyTags - Add tag")?>

    <p class="margins">
        Добавьте тег, по которому Вы хотите получать новости
    </p>
    <div class="button-iconed">
        <i class="fa fa-repeat"></i>
        <input type="text" id="tag-value" class="text-field f-span6 input-large" placeholder="Введите тег и нажмите Enter">
    </div>
    <p id="error-message">
    </p>

<script>
    $(document).ready(function() {
        $("#tag-value").keypress(function(event) {
            if ( event.which == 13 ) {
                $.post(
                    "<?=$this->url(array('controller'=>'index', 'action'=>'addusertag'), 'default', true)?>",
                    { 'tag': $("#tag-value").val() },
                    function(data) {
                        if (data == "OK") {
                            location.href = "following";
                        } else {
                            $("#error-message").html(data);
                        }
                    }
                )
            }
        });
    })
</script>